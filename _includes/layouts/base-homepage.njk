<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body{% if section %} class="{{ section }}"{% endif %}>
    {% include "components/nav-homepage.njk" %}
    {{ layoutContent | safe }}
	</body>

</html>
